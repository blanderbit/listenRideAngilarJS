'use strict';
angular.module('support', []).component('support', {
  templateUrl: 'app/modules/shared/support/support.template.html',
  controllerAs: 'support',
  controller: [
    function SupportController() {
      var support = this;
    }
  ]
});
